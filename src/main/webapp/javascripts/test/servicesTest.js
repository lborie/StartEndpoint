/**
 * Test of the services
 **/

describe('task service test', function () {
    it('adds to number together', function () {
        expect(1 + 2).toEqual(3);
    });
    it('adds to number together again', function () {
        expect(1 + 2).toEqual(3);
    });
});